/**
 * Deprecated — appointment requests now route directly to LINE.
 * Kept as a stub to avoid breaking any stray imports during the redesign.
 */
export default function BookForm() {
  return null;
}
